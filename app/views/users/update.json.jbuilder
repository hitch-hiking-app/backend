json.extract! @user, :first_name, :last_name, :phone,
                     :car_info, :home_city, :license_plate,
                     :license_number, :credit_card_number,
                     :driver
